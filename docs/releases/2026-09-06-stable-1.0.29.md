# DiveFrame stable 1.0.29 — 2026-09-06

Published after maintainer testing of Preview 45.

## Stable source and artifacts

- Source: [`a196becb4ff1c014c99b908e9611607c6757e309`](https://github.com/fishese/diveframe/commit/a196becb4ff1c014c99b908e9611607c6757e309).
- Production tag/release: [`v1.0.29`](https://github.com/fishese/diveframe/releases/tag/v1.0.29).
- Reference workflow: [34029893979](https://github.com/fishese/diveframe/actions/runs/34029893979), completed successfully from the production tag.
- Reference tag/release: [`fdroid-v1.0.29`](https://github.com/fishese/diveframe/releases/tag/fdroid-v1.0.29).
- Reference asset: `diveframe-1.0.29.apk`, `15769121` bytes, SHA-256
  `0E5CD8A1062F1CFF98F2220A0D595B82F67F2CB24E8F024D719FA6202BFF89AD`.
- APK identity: `cc.fishese.divelog`, label `DiveFrame`, version `1.0.29` /
  code `30`, ABI `arm64-v8a`.
- Signature: existing `CN=Fishese` certificate, SHA-256
  `90311d4a659f32a767199164791dba0aa5e05ffa5ed9f73b93baffc9112bb25a`.

Both immutable tags resolve to the exact source above. The production release
has no APK asset; the signed developer reference remains isolated under the
matching `fdroid-v...` release for F-Droid reproducibility checks.

## Validation and publication

`npm test` passed with 275 tests and three optional fixture skips. Lint, native
synchronization, the default local `assembleRelease`, workflow YAML parsing,
and the signed reference workflow passed. The local production manifest and
published signed APK both reported `1.0.29` / code `30`; the signed artifact's
package, label, ABI, certificate, size, and digest were inspected directly.

Feed commit [`8f94834`](https://github.com/fishese/diveframe/commit/8f94834)
advertises stable `1.0.29` / code `30` from the immutable source. Its Cloudflare
Workers deployment succeeded. Preview remains independently recorded as
`preview.45.bbc96f7` / code `100045`.

## F-Droid MR

Both recipe copies point to `1.0.29` / code `30` at the exact stable source.
The existing MR branch was updated by
[`cd7a8a0242231c1c9c691ac2311398e5f94666df`](https://gitlab.com/fishese/fdroiddata/-/commit/cd7a8a0242231c1c9c691ac2311398e5f94666df).
Its recipe diff changes only `versionName`, `versionCode`, `commit`,
`CurrentVersion`, and `CurrentVersionCode`. No MR comment or description change
was made.

The saved `glab` OAuth grant returned `invalid_grant`, so the new GitLab
pipeline and reviewer state could not be read during publication. Restore the
CLI login before recording those as verified.
